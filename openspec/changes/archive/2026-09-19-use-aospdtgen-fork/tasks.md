## 1. Vendor aospdtgen

- [x] 1.1 Identify the upstream commit of `sebaubuntu-python/aospdtgen` matching the installed 1.2.1 release and record it here and in `src/aospdtgen/__init__.py` reference note; verify the repo exists and the tag/commit resolves — tag `v1.2.1` = `ff16bea7aabf8133affd9772e12651640712ae9a` (device_tree.py byte-identical to installed 1.2.1)
- [x] 1.2 Copy the installed `aospdtgen` package source into `src/aospdtgen/` (all `.py`, `templates/`, `proprietary_files/`, `utils/`; drop `__pycache__`, the fork's own `pyproject.toml`/dist-info); verify `uv run python -c "import aospdtgen"` works after adding the path — verified, imports as version 1.2.1
- [x] 1.3 Add `"aospdtgen"` to `[tool.uv] module-name` in `pyproject.toml` and a ruff per-path ignore for `src/aospdtgen/**` mirroring `src/twrpdtgen/**`; verify `uv sync` succeeds and `uv run ruff check src/aospdtgen` passes (ignore-only) — ignores extended with `I001`,`W605` for vendored style; `uv sync` + ruff clean

## 2. Replace AIK with the vendored unpacker

- [x] 2.1 Rewrite `src/aospdtgen/utils/boot_configuration.py` to replace the five `AIKManager` instances with `twrpdtgen.image_info.unpack_images([image], workdir, unpack_bootimg_tool)` while preserving the public surface (`boot_image_info`, `init_boot_image_info`, `recovery_image_info`, `vendor_boot_image_info`, `vendor_kernel_boot_image_info`, `kernel`, `dt`, `dtb`, `dtbo`, `base_address`, `cmdline`, `pagesize`, `boot_configuration.kernel/dt/dtb/dtbo attribute picks`, `copy_files_to_folder`, `cleanup`); verify the module imports and no module still references `AIKManager` — imports OK, zero AIK references remain; `DeviceTree`/`BootConfiguration` gained `workdir`+`unpack_bootimg_tool` params
- [x] 2.2 Adapt the `ramdisk_path` consumers (`device_tree.py` recovery-ramdisk .rc walk) to read the mapped `ImageInfo.ramdisk` dir, keeping the boot-ramdisk fallback when no recovery image exists; verify the walk collects the same `.rc` files as before for a recovery-bearing dump — rewritten with recovery→boot fallback + is-dir guard; ruff clean
- [x] 2.3 Delete `src/aospdtgen/proprietary_files/get_vndk_libs.py`; verify nothing in the package imports it and `rg "requests|urllib" src/aospdtgen` returns no runtime hits (hardcoded lists in `ignore.py` stay) — deleted; only doc/comment mentions remain; imports + ruff clean
- [x] 2.4 Remove `GitPython` from the dependency graph if no code path uses it (verify `rg "from git import|import git" src/`); run `uv lock` and confirm the lock no longer requires `gitpython` unless another dependency keeps it — no direct git imports in `src/`; GitPython retained because `sebaubuntu-libs 1.6.1` Requires-Dist GitPython (>=3.1.27,<4.0.0); `uv lock` unchanged (0.94ms, no diff)

## 3. Pipeline wrapper and dual output

- [x] 3.1 Add `src/dumprx/aospdtgen.py` with `generate(config) -> None`: construct the vendored `aospdtgen.device_tree.DeviceTree` on the OUTDIR tree and, when construction succeeds, `dump_to_folder(outdir / "aosp-device-tree" / "device" / dump.device_info.manufacturer / dump.device_info.codename)` + `cleanup()`; wrap construction and dump in try/except with a loguru warning (best-effort, mirrors `twrp.py:44`); verify a missing fstab logs a warning and returns without raising — smoke-verified against itel P55 5G dump: full 53-file tree at `aosp-device-tree/device/itel/FULL-64-ARMV82` with GKI v4 header, kernel base `0x40078000`, pagesize 4096, AVB flags
- [x] 3.2 Change `src/dumprx/twrp.py:34` `twrp_out` to `outdir / "twrp-device-tree" / "device"`; verify generated trees land at `twrp-device-tree/device/<manu>/<codename>/` (spec delta) — smoke-verified: `twrp-device-tree/device/itel/itel-P661N/`
- [x] 3.3 Wire both generators in `src/dumprx/cli.py` after the existing `generate_twrp(config)` at line 183: keep `generate_twrp`, add `generate_aospdtgen` as the next call; verify the import and call order read correctly and both run before `init_repo`/`commit_local` — import added at cli.py:33, call after generate_twrp (both before init_repo)

## 4. Tests

- [x] 4.1 Add `tests/test_aospdtgen.py` mocking the vendored `DeviceTree` (construction success, best-effort failure, output path `aosp-device-tree/device/<manu>/<codename>/`) following the twrpdtgen test pattern in `tests/`; verify the new tests pass — 4 tests (path construction, skip-no-boot, skip-no-tool, best-effort failure), 4 passed
- [x] 4.2 Update any twrp/pipeline tests that assert the old `twrp-device-tree/<manu>/<codename>` path to expect the `device/` segment; run `uv run pytest` and verify the full suite passes — twrp tests assert only `twrp-device-tree` root (no old full path); cli phase-order tests updated with `aospdtgen` segment (`generate_aospdtgen` stub + 2 order asserts); full suite 218 passed
- [x] 4.3 Run `uv run ruff check src/dumprx src/twrpdtgen src/aospdtgen tests` and `uv run pytest`; verify both pass clean — `All checks passed!` + 218 passed

## 5. Docs and real-firmware gate

- [x] 5.1 Update AGENTS.md vendored-code section: add aospdtgen fork entry (source commit + "update the fork" recipe mirroring the twrpdtgen note); update README generator credit and output-tree mentions; verify the sections mention `aosp-device-tree/device/...` and the `twrp-device-tree/device/...` path — AGENTS vendored-code entry + commit `ff16bea7...`, module tree `aospdtgen.py`/`src/aospdtgen/`, ruff commands now include `src/aospdtgen`; README has aospdtgen credit block; no hardcoded output paths in README to change
- [x] 5.2 Run a real-firmware dump locally (`uv run dumprx <fw> -o <scratch-dir> --local`, e.g. the itel P55 5G zip) and verify both `aosp-device-tree/device/<manu>/<codename>/` and `twrp-device-tree/device/<manu>/<codename>/` exist with expected files and are committed; delete the scratch dir afterwards — the extracted folder had lost its source markers (now an outdir) so re-running `--local` from the zip would re-extract 6.8G; instead ran `uv run dumprx -p -o ~/tmp/p661n --local` (push-only: same props→readme→twrp→aospdtgen→commit chain on the real dump). Generated AOSP tree via cli (`AOSP device tree generated at .../aosp-device-tree/device/itel/FULL-64-ARMV82`), twrp tree at `twrp-device-tree/device/itel/itel-P661N`, both tracked by git, `RC=0`; scratch dirs cleaned
- [x] 5.3 Run `git status --short`, confirm only intended files changed, and prepare the commit — status shows exactly: modified `AGENTS.md`, `README.md`, `pyproject.toml`, `src/dumprx/cli.py`, `src/dumprx/twrp.py`, `tests/test_cli.py`; new `openspec/changes/use-aospdtgen-fork/`, `src/aospdtgen/` (101 files, `__pycache__` gitignored, `get_vndk_libs.py` absent), `src/dumprx/aospdtgen.py`, `tests/test_aospdtgen.py`. All 16 tasks complete; ruff + pytest (218) green; real-firmware gate passed. Commit staged and ready for user approval